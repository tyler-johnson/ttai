name: Build

on:
  release:
    types: [created]
  push:
    branches: [main]
    paths:
      - 'src-go/go.sum'
      - 'src-go/go.mod'
  workflow_dispatch:

jobs:
  build-macos:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            arch: arm64
            goarch: arm64
          - os: macos-15
            arch: amd64
            goarch: amd64

    runs-on: ${{ matrix.os }}
    name: Build macOS (${{ matrix.arch }})
    env:
      HAVE_SIGNING_SECRETS: ${{ secrets.APPLE_CERTIFICATE_BASE64 != '' }}
      HAVE_NOTARIZATION_SECRETS: ${{ secrets.APPLE_API_KEY_BASE64 != '' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src-web/package-lock.json

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache-dependency-path: src-go/go.sum

      - name: Extract version from release tag
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="0.0.0-dev"
          fi
          echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building version: $VERSION"

      - name: Import Code Signing Certificate
        if: github.event_name == 'release' && env.HAVE_SIGNING_SECRETS == 'true'
        env:
          APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          # Create temporary keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -lut 21600 build.keychain

          # Import certificate
          echo "$APPLE_CERTIFICATE_BASE64" | base64 --decode > certificate.p12
          security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain
          rm certificate.p12

      - name: Setup notarization credentials
        if: github.event_name == 'release' && env.HAVE_NOTARIZATION_SECRETS == 'true'
        env:
          APPLE_API_KEY_BASE64: ${{ secrets.APPLE_API_KEY_BASE64 }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
        run: |
          mkdir -p ~/private_keys
          echo "$APPLE_API_KEY_BASE64" | base64 --decode > ~/private_keys/"AuthKey_${APPLE_API_KEY_ID}.p8"

      - name: Build, sign, and bundle
        working-directory: src-go
        env:
          CGO_ENABLED: 1
          GOOS: darwin
          GOARCH: ${{ matrix.goarch }}
          MACOSX_DEPLOYMENT_TARGET: '11.0'
          # Only set CODESIGN_IDENTITY on release builds (when certificate is imported)
          CODESIGN_IDENTITY: "${{ github.event_name == 'release' && env.HAVE_SIGNING_SECRETS == 'true' && format('Developer ID Application: {0}', secrets.APPLE_TEAM_NAME) || '' }}"
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          make VERSION="$VERSION" zip-darwin-${{ matrix.arch }}

      - name: Notarize app
        if: github.event_name == 'release' && env.HAVE_NOTARIZATION_SECRETS == 'true'
        working-directory: src-go
        env:
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
        run: |
          # Submit for notarization
          xcrun notarytool submit dist/TTAI-darwin-${{ matrix.arch }}.zip \
            --key ~/private_keys/"AuthKey_${APPLE_API_KEY_ID}.p8" \
            --key-id "$APPLE_API_KEY_ID" \
            --issuer "$APPLE_API_ISSUER_ID" \
            --wait

          # Unzip, staple the notarization ticket, and re-zip
          cd dist
          ditto -x -k TTAI-darwin-${{ matrix.arch }}.zip .
          rm TTAI-darwin-${{ matrix.arch }}.zip
          xcrun stapler staple TTAI.app
          ditto -c -k --keepParent TTAI.app TTAI-darwin-${{ matrix.arch }}.zip

      - name: Clean up keychain and credentials
        if: always() && github.event_name == 'release'
        run: |
          security delete-keychain build.keychain 2>/dev/null || true
          rm -rf ~/private_keys 2>/dev/null || true

      - name: Upload artifact
        if: github.event_name == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: TTAI-darwin-${{ matrix.arch }}
          path: src-go/dist/TTAI-darwin-${{ matrix.arch }}.zip
          if-no-files-found: error

  build-windows:
    runs-on: windows-latest
    name: Build Windows (amd64)

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src-web/package-lock.json

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache-dependency-path: src-go/go.sum

      - name: Extract version from release tag
        id: version
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="0.0.0-dev"
          fi
          echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building version: $VERSION"

      - name: Install make
        run: choco install make -y

      - name: Build
        working-directory: src-go
        shell: bash
        env:
          CGO_ENABLED: 1
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          make VERSION="$VERSION" build-windows

      - name: Install WiX Toolset
        if: github.event_name == 'release'
        run: |
          choco install wixtoolset -y
          echo "C:\Program Files (x86)\WiX Toolset v3.14\bin" >> $GITHUB_PATH
        shell: bash

      - name: Install go-msi
        if: github.event_name == 'release'
        run: go install github.com/mh-cbon/go-msi@latest
        shell: bash

      - name: Build MSI
        if: github.event_name == 'release'
        working-directory: src-go
        shell: pwsh
        run: |
          $VERSION = "${{ steps.version.outputs.VERSION }}"
          # go-msi make - output MSI to tmp dir then copy
          New-Item -ItemType Directory -Force -Path "go-msi-tmp"
          go-msi make --path wix.json --src templates --msi go-msi-tmp/TTAI-Setup.msi --version $VERSION --out go-msi-tmp
          Copy-Item "go-msi-tmp/TTAI-Setup.msi" -Destination "dist/TTAI-Setup.msi"

      - name: Upload EXE artifact
        if: github.event_name == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: TTAI-windows-exe
          path: src-go/dist/TTAI-windows.exe
          if-no-files-found: error

      - name: Upload MSI artifact
        if: github.event_name == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: TTAI-windows-msi
          path: src-go/dist/TTAI-Setup.msi
          if-no-files-found: warn

  build-linux:
    runs-on: ubuntu-22.04
    name: Build Linux (amd64)

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src-web/package-lock.json

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache-dependency-path: src-go/go.sum

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev \
            xorg-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxi-dev \
            libxxf86vm-dev

      - name: Extract version from release tag
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="0.0.0-dev"
          fi
          echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building version: $VERSION"

      - name: Build
        working-directory: src-go
        env:
          CGO_ENABLED: 1
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          make VERSION="$VERSION" build-linux

      - name: Install AppImageTool
        if: github.event_name == 'release'
        run: |
          wget -q https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage -O appimagetool
          chmod +x appimagetool
          sudo mv appimagetool /usr/local/bin/appimagetool

      - name: Build AppImage
        if: github.event_name == 'release'
        working-directory: src-go
        env:
          ARCH: x86_64
          VERSION: ${{ steps.version.outputs.VERSION }}
        run: |
          # Update version in desktop file
          sed -i "s/X-AppImage-Version=.*/X-AppImage-Version=$VERSION/" ttai.desktop
          make VERSION="$VERSION" appimage

      - name: Upload binary artifact
        if: github.event_name == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: TTAI-linux-binary
          path: src-go/dist/TTAI-linux
          if-no-files-found: error

      - name: Upload AppImage artifact
        if: github.event_name == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: TTAI-linux-appimage
          path: |
            src-go/dist/TTAI-x86_64.AppImage
            src-go/dist/TTAI-x86_64.AppImage.zsync
          if-no-files-found: warn

  release:
    if: github.event_name == 'release'
    needs: [build-macos, build-windows, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Upload artifacts to release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
